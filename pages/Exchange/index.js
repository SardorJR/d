import{
    navBar,
    createHeader
} from "../../modules/ui.js"

let leaft_bar=document.querySelector('.leaft_bar')
navBar(leaft_bar)
let header=document.querySelector('header')
createHeader(header)